apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-e2e-kind.fullname" . }}-test-connection"
  labels:
    {{- include "example-e2e-kind.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-e2e-kind.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
